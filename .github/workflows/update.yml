name: Data update and deploy

on:
  schedule:
    - cron: "15 0-8 * * 1-5" # roughly when game updates happen
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  update-data:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Ingest data from mirror
        run: npm run ingest

      - name: Commit and push collection changes
        id: commit_changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add -f src/_data/ .fetch-state.json
          if git diff --staged --quiet; then
            echo "No collection changes to commit"
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            VERSION_TRAILERS=$(node .github/workflows/update-version-trailers.js)
            git commit -m "Update data" -m "$VERSION_TRAILERS"
            git push
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Test, build, and verify runtime fetch policy
        run: npm run ci

      - name: Setup Pages
        uses: actions/configure-pages@v4
        with:
          enablement: true

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./_site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
